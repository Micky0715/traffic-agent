# Phase 2A-1 结果：`` 中文边界 Bug 修复

run_id：`phase2a1-run1-20260915-1947`　**这是第一次也是唯一一次正式运行，结果永久保存，未重跑、未调参。**

预注册文件：`docs/phase2a1_prediction.md`（写于任何代码改动之前）

## 1. 改动

仅 `src/extractors.py`（改动后 sha256 `fdaf05a739ce63823fedcba2cbaa8f26…`）：

1. `REG_PATTERN` / `DRAWING_PATTERN` 首尾的 `` → `(?<![A-Za-z0-9])` / `(?![A-Za-z0-9])`
2. 伪 asset 剔除从「压缩字符串包含」改为「匹配 span 重叠」，并首次覆盖图号侧

冻结校验在改动后精确报出且**仅**报出 `CHANGED routing_runtime_code: src/extractors.py`，与预注册第 7 节一致。

## 2. 指标（challenge75，回归验证集）

| 路由 | 指标 | 修复前 | 修复后 | 变化 |
|---|---|---|---|---|
| v1_single_label | 端到端 | 29.3% | **32.0%** | +2.7pp |
| v1_single_label | 槽位覆盖(代理) | 90.7% | 93.3% | +2.7pp |
| v2_multi_intent | 端到端 | 36.0% | **38.7%** | +2.7pp |
| v2_multi_intent | 槽位覆盖(代理) | 86.7% | 89.3% | +2.7pp |
| v3_guarded | 端到端 | 49.3% | **52.0%** | +2.7pp |
| v3_guarded | 槽位覆盖(代理) | 86.7% | 89.3% | +2.7pp |

**意图集合、意图 F1、工具集合、工具 F1、澄清准确率、执行模式准确率：六项全部逐位不变。**

base46：V1/V2/V3 **全部零变化**（V2/V3 本就 100% 饱和）。

## 3. 预期 vs 实际

预注册预期 7 条好转，**实际只修好 2 条**：

| case | 预期 | 实际 | 真实原因 |
|---|---|---|---|
| B11 | 好转 | ✅ **修好** | 尾部 `` 后跟中文「对」，本次修复命中 |
| B44 | 好转 | ✅ **修好** | 同上 |
| B13 | 好转 | ❌ 仍失败 | **同一个 `` bug，但在 `normalize.py:13`**，不在本次改动范围 |
| B16 | 好转 | ❌ 仍失败 | **同上**，且更糟：尾部 `` 失败后正则回溯，把 `TB10621-2014` 规范化成了 `TB 1-0621-2014` |
| B12 | 好转 | ❌ 仍失败 | 全角字符 `ＪＴＧ Ｄ８１` 未做 NFKC 折叠 |
| B15 | 好转 | ❌ 仍失败 | 全角连字符 `－`(U+FF0D) 不在 `normalize.DASHES` 里 |
| B71 | 好转 | ❌ 仍失败 | 阈值词「达到」不在 `超过\|高于\|大于\|低于\|小于` 列表内 |

**预期落空 5/7。** 我在预注册时假定 7 条同源，实际只有 2 条真正源于 `extractors.py` 的 ``。

## 4. 预期的负面影响没有发生

预注册第 5 节预告：伪 asset 消失可能改变 `missing_slots` → 改变 clarify → 让意图/工具/端到端**变差**。

**实测未发生**：新失败 0 条，六项指标零变化。
原因是 `D81` 这类伪 asset 只出现在 `regulation_lookup` 子任务上，而该意图的 `required_slots` 为空，
删掉它不触发任何 `missing_slots` 变化。**预告的风险是合理的，只是这批数据没有踩到。**

## 5. 本轮最重要的发现

**`` + 中文的 bug 存在于两个文件，本轮只修了一个。**

`src/normalize.py:13`：
```python
re.sub(r"(JTG|TB|GB|CJJ)\s*([A-Z]?\d+[A-Z]?)\s*-?\s*(\d{4})", r" -", text)
```
尾部 `` 后跟中文时同样失配。B13 因此没被规范化成 `JTG D81-2017`；
B16 更严重——回溯产生了**错误的**规范化结果 `TB 1-0621`，即**规范化过程本身损坏了数据**。

**本轮不修它。** 在看到 challenge75 结果之后再改正则，就是拿测试集调参。
应作为 Phase 2A-2 单独预注册，单独运行一次。

## 6. 诚实结论

- 端到端 **49.3% → 52.0%（+2.7pp）**，三个路由一致，仅来自 B11 与 B44 两条；
- 这是**真 bug 修复**，不是调参，也没有改任何规则、阈值或关键词；
- 但**修复覆盖面远小于我的预期**（2/7 而非 7/7），预注册中的同源假设是错的；
- 六项主要指标不变，说明本次修复**只影响槽位抽取，不影响意图与工具判定**。
